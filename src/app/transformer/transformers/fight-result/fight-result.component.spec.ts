import { FightResultComponent } from './fight-result.component';

describe('FightResultComponent', () => {
  let component: FightResultComponent;

  beforeEach(() => {
    component = new FightResultComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
